{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python Debugger: FastAPI (cwd=app)",
            "type": "debugpy",
            "request": "launch",
            "module": "uvicorn",
            "args": [
                "server:app",
                "--reload"
            ],
            "cwd": "${workspaceFolder}/app",
            "jinja": true
        }
    ]
}
